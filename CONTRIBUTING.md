# 贡献指南（CONTRIBUTING.md）

欢迎贡献你的同事。

本项目的一切都围绕一个原则：**逻辑与内容分离**——你只改数据，不碰代码。核心引擎（机制、数值、状态机）由 maintainer 维护，你负责让这个世界更真实。

## 你能贡献什么

| 贡献 | 难度 | 价值 |
|---|---|---|
| 语录 | 30秒 | 最高——真实语录是本项目的传播货币 |
| 新角色 | 会写JSON就行 | 高——全世界的同事都在等你 |
| 机制建议 | 开issue聊 | 中 |
| 文案润色 | 直接提PR | 中 |

## 贡献语录（最简单的姿势）

1. 打开 `quotes.json`
2. 找到对应场景：`question`（质疑）/ `kick`（踢皮球）/ `debate_lv1~4`（辩论理由升级）/ `breakdown`（破防）/ `small_group`（小群）/ `freeze`（摸鱼）/ `refuse`（拒绝）/ `xxx_normal`（日常）
3. 在数组里加一条字符串，口语化，带语气毛边
4. 没有对应场景？新建一个场景key
5. 提PR，描述里写一句："这句来自我前同事XX"

规则：

- 必须口语化。**禁客服腔**（"收到您的需求"这种会被打回）
- 真实语录优先，编的也可以，但要有梗
- 涉及人名一律用职位替代（开发哥/测试姐/产品），**不夹带真实人名和公司名**

## 新增角色（会写JSON就行）

在 `agents.json` 的 `roles` 里加一个对象：

```json
"你的角色id": {
  "id": "你的角色id",
  "name": "角色名",
  "trait": "一句话性格",
  "anger_speed": 8,
  "behavior": {
    "question_threshold": 40,
    "kick_threshold": 70,
    "refuse_threshold": 70,
    "group_threshold": 90
  },
  "personality": {
    "口癖": ["……", "……"],
    "说话习惯": "一句话描述他怎么说话",
    "小动作": "一句话描述他的肢体语言",
    "提醒": "扮演要点，防止演歪"
  }
}
```

字段说明：

- `id`：唯一标识
- `name`：显示名
- `trait`：性格标签
- `anger_speed`：不满涨速（数字；或 `"follow"` 表示跟随小群）
- `behavior`：行为阈值（`question`质疑 / `kick`踢皮球 / `refuse`拒绝 / `group`拉群）
- `personality`：活人感三件套（口癖/说话习惯/小动作）+ 扮演提醒

## 提PR流程

1. fork 本仓库
2. 改数据文件（quotes.json / agents.json）
3. 提 Pull Request
4. 等合并——你的同事永久进入档案馆

## Issue规范

- 标题写清场景，例如："语录：测试姐式阴阳怪气"
- 正文给台词原文 + 场景描述
- 真实语录标注来源类型（前同事/现任/网梗）

## 别做什么

- 别改 SKILL.md 的机制和数值（那是maintainer的事）
- 别用AI味文案（"赋能/闭环"直接打回）
- 别夹带真实人名和公司名（保护隐私，也保护你）
